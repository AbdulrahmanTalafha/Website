interface Props {
  darkHref: string
  lightHref: string
  classicHref: string
  current: 'dark' | 'light' | 'classic'
  isRTL: boolean
}

export default function DesignSwitcher(_props: Props) {
  return null
}
