# Copy to .env.docker for docker compose (optional — compose sets these by default):
# cp .env.example.docker .env.docker

NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_CMS_URL=http://localhost:8000

# Server-side API calls from the frontend container to CMS on the host (port 8000)
CMS_INTERNAL_URL=http://host.docker.internal:8000
